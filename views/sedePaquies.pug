extends ./layoutSedes.pug

block contenido
	div(class="container")
		div(class="col-sm-12")
			div(class="tab-pane fade show" id="list-sede1" role="tabpanel" aria-labelledby="list-sede1-list")  
				h1(class="centrar") Bienvenido a las votaciones Sede Paquies
				div(class="separar")
				h3 Seleccione un grado
				div(class="separar")
			form(action="/votarSedePaquies" method="POST")
				div(class="row")
					div(data-toggle="radio" class="grupoGrados")
						input(type="submit" class="btn select" value="GRADO 0" name="gradosSedePaquies" required)
						input(type="submit" class="btn select" value="PRIMERO" name="gradosSedePaquies" required)
						input(type="submit" class="btn select" value="SEGUNDO" name="gradosSedePaquies" required)
						input(type="submit" class="btn select" value="TERCERO" name="gradosSedePaquies" required)
						input(type="submit" class="btn select" value="CUARTO" name="gradosSedePaquies" required)
						input(type="submit" class="btn select" value="QUINTO" name="gradosSedePaquies" required)
